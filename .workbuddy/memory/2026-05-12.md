# 2026-05-12 日志

## homepage-content-sync 技能完成

创建了 `~/.workbuddy/skills/homepage-content-sync/` 技能，包含：

- `SKILL.md` — 技能说明
- `scripts/sync_homepage.py` — 同步脚本 v2

### 博客路径
- 博客根目录：`/Users/a1-6/WorkBuddy/20260313105301/`
- 远程：`git@github.com:Drrreistein/felix-blog.git` (master分支)

### 新增三项功能
1. **增量同步**：通过 `.synced_cache.json` (mtime+hash) 实现，未变文件跳过 hash 计算
2. **Dry-run 模式**：`--dry-run` 参数只打印操作不实际执行
3. **YouTube 缩略图预检**：用 curl 检查 maxresdefault 是否存在，不存在直接用 hqdefault

### 初始化状态
- kami 源：26 个 HTML，21 个已存在于 dest（手动同步遗留），5 个新增
- podcast 源：23 个 HTML，全部已同步
- 缓存文件：kami 和 podcast 各自源目录下的 `.synced_cache.json`
